import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Extract Form Data · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="forms-extract" />;
}
