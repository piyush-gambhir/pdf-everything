import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Reorder Pages · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="reorder" />;
}
