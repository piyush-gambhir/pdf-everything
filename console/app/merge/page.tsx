import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Merge PDFs · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="merge" />;
}
