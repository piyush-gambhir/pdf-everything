import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Edit Metadata · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="metadata" />;
}
