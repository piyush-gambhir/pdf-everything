import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Crop PDF · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="crop" />;
}
