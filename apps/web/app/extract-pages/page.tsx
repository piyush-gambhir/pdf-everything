import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Extract Pages · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="extract-pages" />;
}
