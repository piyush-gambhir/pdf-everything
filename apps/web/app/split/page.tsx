import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Split PDF · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="split" />;
}
