import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Remove Pages · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="remove-pages" />;
}
