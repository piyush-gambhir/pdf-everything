import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Images to PDF · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="images-to-pdf" />;
}
