import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Page Numbers · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="page-numbers" />;
}
