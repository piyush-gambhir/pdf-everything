import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Resize Pages · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="resize-pages" />;
}
