import { ToolLayout } from '@/components/tools/tool-layout';

export const metadata = { title: 'Rotate PDF · pdf-everything' };

export default function Page() {
  return <ToolLayout toolId="rotate" />;
}
